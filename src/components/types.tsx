// BLOG
export type BlogTagType = {
  name: string;
};

// THEME
export type BackgroundGutters = { l: number; s: number };
export type Frames = { outline: { border: number } };

// TOP NAVIGATION
export type NavLink = { url: string; name: string };
